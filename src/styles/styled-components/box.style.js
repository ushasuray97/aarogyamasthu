import Box from "@mui/material/Box";
import {styled}from '@mui/material';


export const CustomMainBox = styled(Box)(({ theme }) => ({
    width: "100%",
    backgroundColor: "#f5f5f5",
    height: "auto"
  }));